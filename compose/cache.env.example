# Redis password for the akeyless-cache service.
# Copy this file to cache.env and set a strong password before first start.
# Redis is bound to 127.0.0.1 only, so this protects the local host and any
# container that reads this file; it is still a credential, treat it as one.
REDIS_PASS="change-this-redis-password"
